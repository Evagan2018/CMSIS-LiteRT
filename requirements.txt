# Runtime dependencies for create_ai_layer.py: Ethos-U Vela compiles the model,
# while PyYAML reads its configuration from *.cbuild-mlops.yml. 
# Model-training dependencies, including TensorFlow, are listed in Training/requirements.txt.
ethos-u-vela==5.1.0
PyYAML>=6
